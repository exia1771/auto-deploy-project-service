<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="github.exia1771.deploy.core.mapper.ProjectUserMapper">

    <insert id="batchAddProjectMember" parameterType="java.util.List">
        insert into project_user
        values (id, project_id, user_id, creator_id, creation_time, updater_id, update_time)

        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.id},
            #{item.projectId},
            #{item.userId},
            #{item.creatorId},
            now(),
            #{item.updaterId},
            now()
            )
        </foreach>

    </insert>

    <delete id="batchRemoveProjectMember" parameterType="java.util.List">

        delete from project_user
        where (project_id, user_id) in
        <foreach open="(" close=")" collection="list" item="item" index="index" separator="or">
            (#{item.projectId}, #{item.userId})
        </foreach>

    </delete>

</mapper>
